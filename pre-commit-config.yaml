repos:
-   repo: local
    hooks:
    - id: requirements
      name: requirements
      language: system
      entry: bash -C "venv/Scripts/pip freeze > requirements.txt; git add requirements.txt"
      always_run: true
      pass_filenames: false
      stages: [commit]

   